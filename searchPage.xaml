<Page x:Class="Onigaku.searchPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Onigaku"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="searchPage">

    <Grid Background="#171717">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button Content="&#xE751;" FontFamily="Segoe MDL2 Assets" Foreground="White" FontSize="30" HorizontalAlignment="Left" Height="59" VerticalAlignment="Top" Width="99" Opacity="0.5" Background="#171717" BorderBrush="Transparent" Click="search_MoveBack"/>
        <ListBox Margin="284,0,0,-0.4"/>
        
                    <StackPanel Height="60" Width="100" Orientation="Horizontal">
                        <TextBlock Height="60" Text="&#xE721;" FontFamily="Segoe MDL2 Assets" Margin="0, 3, 15, 0"/>
                        <Button Height="60" Content="Искать"/>
                    </StackPanel>
        <TextBlock VerticalAlignment="Center" Foreground="Gray" Background="White" TextWrapping="Wrap" Margin="10,180,648,247.6" Height="22"><Run Text="Введите название трека"/>
        </TextBlock>
        
    </Grid>
</Page>
